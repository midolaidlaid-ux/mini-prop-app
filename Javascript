import TelegramBot from 'node-telegram-bot-api';

const token = '8509851536:AAHTzXYmumV6DUmYffh_ptxam0LE5dhdcSE';
const bot = new TelegramBot(token, { polling: true });

// رابط النسخة الأصلية للمشروع
const appLink = 'https://yourdomain.com/index.html'; // ضع رابط النسخة الأصلية بعد رفعها

bot.onText(/\/start/, (msg) => {
    const chatId = msg.chat.id;
    const welcomeMsg = `مرحبًا بك في Prop Challenge!\nاضغط على الرابط للبدء:`;
    bot.sendMessage(chatId, welcomeMsg, {
        reply_markup: {
            inline_keyboard: [
                [{ text: "ابدأ التحدي", url: appLink }]
            ]
        }
    });
});

bot.onText(/\/help/, (msg) => {
    bot.sendMessage(msg.chat.id, "اضغط على 'ابدأ التحدي' للانتقال إلى الموقع والدخول في التحدي.");
});
